services:
  - type: web
    name: finwell-ai-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: GEMINI_API_KEY
        sync: false          # you paste this manually in the Render dashboard
      - key: DATABASE_URL
        sync: false          # paste your Neon/Supabase/Aiven URL here
